{
  "name": "kafka-demo-app",
  "version": "1.0.0",
  "description": "Demo application for OpenShift Pipeline",
  "main": "app.js",
  "scripts": {
    "start": "node app.js",
    "test": "echo 'Tests passed' && exit 0"
  },
  "dependencies": {
    "express": "^4.17.1"
  }
}
